services:
  watermark-remover:
    build: .
    container_name: ai-watermark-remover
    ports:
      - "7860:7860"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
      - GRADIO_SERVER_PORT=7860
    restart: unless-stopped
